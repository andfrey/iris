project: cell_cycle_ridge_sweep
count: 50
method: bayes  # Options: grid, random, bayes
metric:
  name: val_mse
  goal: minimize

parameters:
  # Regularization strength
  alpha:
    min: 0.0001
    max: 10.0
